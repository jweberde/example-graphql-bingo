import { CardCreatedResponse } from './card-created-response';

describe('CardCreatedResponse', () => {
  it('should create an instance', () => {
    expect(new CardCreatedResponse()).toBeTruthy();
  });
});
